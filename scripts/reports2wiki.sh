#!/bin/sh
# LEGACY — original paths (/mnt/2/development/...) no longer exist.
# Kept for reference only; do not schedule without updating paths.

echo "LEGACY: reports2wiki.sh references removed paths."
echo "Also had broken date syntax: date( 'Ymd' ) instead of date +%Y%m%d"
exit 1
