//This file was generated by calling php /var/www/html/finance/generator2.php investing 

class evalfinancial extends generictable 
{
         private:
         char* observers;
         char pretty_name[] = '';
         char pretty_plural[] = '';
         char pageheader[] = '';
         char pagefooter[] = '';
         char *data; //data passed in by other calls
         function evalfinancial()
         {
         	strcpy( this->querytablename, "evalfinancial" ) 
         
	         return SUCCESS;
         }
         function ObserverRegister( OBSERVER *observer, int event )
         {
                 this->observers[event][] = observer;
	         return SUCCESS;
         }
         function ObserverDeRegister( OBSERVER *observer )
         {
                 this->observers[] = array_diff( this->observers, array( observer) );
	         return SUCCESS;
         }
         function ObserverNotify( int event, char *msg )
         {
                 if ( isset( this->observers[event] ) )
                 	foreach ( this->observers[event] as obs ) 
                 	{
                      		obs->notify( event, msg );
                 	}
                 /* '0' being used as 'ALL' */
                 if ( isset( this->observers[0] ) )
                 	foreach ( this->observers[0] as obs ) 
                 	{
                      		obs->notify( event, msg );
                 	}
	         return SUCCESS;
         }
         function notify( object )
         {
                 //Called when another object we are observing sends us a notification
	         return SUCCESS;
         }
} /* class evalfinancial */
